import React from 'react';
import { Typography } from '@mui/material';

// TODO: Import text from storage
export const Header = () => (
  <Typography variant={'h1'} marginBottom={'2rem'}>Welcome to Misx Fox&apos;s Den</Typography>
);